[Unit]
Description=kube-apiserver
Documentation=https://k8s.io
After=network.target

[Service]
Type=notify
User=kubernetes
ExecStart=<%= @kube_apiserver_command %>

[Install]
WantedBy=multi-user.target
